anthropic>=0.40.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
httpx>=0.28.0
pydantic>=2.0.0
email-validator>=2.2.0
# Auth maison (cf. backend/auth.py) — PyJWT signe les jetons, argon2-cffi hashe
# les mots de passe. cryptography n'est pas utilisé directement mais PyJWT le
# réclame dès qu'on sort de HS256 ; il était déjà dans le venv sans être déclaré.
PyJWT>=2.10.0
argon2-cffi>=23.1.0
cryptography>=44.0.0
# Paiement (cf. backend/payments.py). Sans STRIPE_SECRET_KEY, les routes de
# paiement répondent 503 et le reste du produit fonctionne normalement.
stripe>=15.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# Export WordPress (cf. pipeline/wordpress_export.py) — l'API REST de WordPress
# attend du HTML, le pipeline produit du markdown. Extension `tables` requise :
# les articles en contiennent souvent, et un tableau non converti part en texte.
markdown>=3.7
rq>=1.16.0
redis>=5.0.0
asyncpg>=0.30.0
tenacity>=9.0.0
python-slugify>=8.0.0
